# 🚀 سكريبت النشر السريع لمشروع V5 على Ubuntu 25

## ⚡ البدء السريع

```bash
# 1. انسخ الملفات إلى الخادم
scp -r V5/ root@your-server:/tmp/

# 2. شغل السكريبت على الخادم
ssh root@your-server
cd /tmp/V5
chmod +x deploy-ubuntu25.sh
./deploy-ubuntu25.sh
```

## ✅ ما يقوم به السكريبت

- 🔧 **تثبيت PHP 8.2** مع جميع المكونات المطلوبة
- 🗄️ **إعداد MySQL** مع قاعدة بيانات آمنة
- 🌐 **تثبيت Nginx** مع تحسينات الأداء
- 🔒 **SSL مجاني** من Let's Encrypt
- 📦 **Node.js 22** للواجهة الأمامية
- 🔴 **Redis** للذاكرة المؤقتة
- 🛡️ **Firewall + Fail2ban** للحماية
- 💾 **نسخ احتياطية تلقائية** يومياً
- 🎛️ **سكريبت إدارة** سهل الاستخدام
- ⚡ **تثبيت سريع بدون ترقية النظام** (توفير الوقت)

## 🎯 النتيجة النهائية

- ✅ موقع آمن جاهز للإنتاج
- ✅ لوحة إدارة: `https://your-domain.com/admin`
- ✅ SSL مُفعل تلقائياً
- ✅ أداء محسن مع Caching
- ✅ نسخ احتياطية يومية
- ✅ مراقبة أمنية شاملة

## 📖 دليل شامل

للحصول على دليل شامل يحتوي على:
- حل المشاكل الشائعة
- إعدادات متقدمة
- نصائح الأمان والأداء
- أوامر الإدارة

اقرأ ملف: `DEPLOYMENT_GUIDE.md`

## 🆘 مساعدة سريعة

```bash
# فحص حالة النظام
sudo v5-control status

# عرض السجلات
sudo v5-control logs

# إعادة تشغيل
sudo v5-control restart
```

---
**📅 تاريخ الإنشاء**: 2025-11-13  
**🔢 الإصدار**: 1.0  
**🖥️ المنصة**: Ubuntu 25 + PHP 8.2 + MySQL 8.0 + Nginx