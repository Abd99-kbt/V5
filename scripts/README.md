# Production Scripts Documentation

## نظرة عامة
هذا المجلد يحتوي على السكريبتات المساعدة وأدوات التشغيل للنظام الإنتاجي Laravel.

---

## 📋 قائمة السكريبتات

### 1. production-helper.sh
**الوصف:** سكريبت مساعد شامل لإدارة النظام الإنتاجي
**الاستخدام:**
```bash
# عرض الحالة الشاملة
./production-helper.sh status

# فحص قاعدة البيانات فقط
./production-helper.sh database

# فحص التخزين المؤقت
./production-helper.sh cache

# فحص الخدمات
./production-helper.sh services

# فحص موارد النظام
./production-helper.sh resources

# فحص الأمان
./production-helper.sh security

# فحص الأداء
./production-helper.sh performance

# تحليل السجلات
./production-helper.sh logs

# معلومات النظام
./production-helper.sh system

# قائمة الإصلاحات السريعة
./production-helper.sh fix

# تشغيل جميع الفحوصات
./production-helper.sh all

# عرض المساعدة
./production-helper.sh help
```

### 2. production-backup.sh
**الوصف:** نظام نسخ احتياطي متقدم وشامل
**الاستخدام:**
```bash
# نسخة احتياطية كاملة
./production-backup.sh full

# نسخة احتياطية لقاعدة البيانات فقط
./production-backup.sh database

# نسخة احتياطية للملفات فقط
./production-backup.sh files

# نسخة احتياطية للإعدادات فقط
./production-backup.sh config

# عرض المساعدة
./production-backup.sh help
```

### 3. production-restore.sh
**الوصف:** نظام استعادة النسخ الاحتياطية
**الاستخدام:**
```bash
# عرض النسخ الاحتياطية المتاحة
./production-restore.sh list

# استعادة نسخة احتياطية كاملة
./production-restore.sh laravel-app_full_20241106_120000

# استعادة قاعدة البيانات فقط
./production-restore.sh laravel-app_full_20241106_120000 --type database

# استعادة الملفات فقط
./production-restore.sh laravel-app_full_20241106_120000 --type files

# عرض المساعدة
./production-restore.sh help
```

### 4. production-monitor.sh
**الوصف:** نظام مراقبة شامل للأداء والحالة
**الاستخدام:**
```bash
# عرض الحالة الحالية
./production-monitor.sh status

# تشغيل دورة مراقبة واحدة
./production-monitor.sh once

# عرض المساعدة
./production-monitor.sh help
```

---

## 🔧 إعداد السكريبتات

### 1. إعطاء صلاحيات التنفيذ
```bash
# إعطاء صلاحيات التنفيذ لجميع السكريبتات
chmod +x *.sh
```

### 2. تحديث متغيرات التكوين
قم بتحديث المتغيرات التالية في بداية كل سكريبت:

```bash
# المسارات والإعدادات الأساسية
APP_NAME="laravel-app"
APP_PATH="/var/www/your-app"
BACKUP_DIR="/var/backups/${APP_NAME}"
RETENTION_DAYS=30

# إعدادات قاعدة البيانات
MYSQL_USER="app_user"
MYSQL_PASSWORD="strong_password"
MYSQL_DATABASE="production_app"
```

---

## 📊 الاستخدام اليومي

### الروتين اليومي
```bash
# 1. فحص حالة النظام
./production-helper.sh status

# 2. تشغيل مراقبة واحدة
./production-monitor.sh once

# 3. إنشاء نسخة احتياطية دورية
./production-backup.sh database
```

### فحص أسبوعي
```bash
# فحص شامل للنظام
./production-helper.sh all

# تنظيف السجلات القديمة
./production-monitor.sh cleanup

# فحص النسخ الاحتياطية
./production-restore.sh list
```

### الصيانة الشهرية
```bash
# نسخة احتياطية كاملة
./production-backup.sh full

# فحص إعدادات الأمان
./production-helper.sh security

# تحليل أداء شامل
./production-helper.sh performance
```

---

## ⚠️ تحذيرات مهمة

### 1. صلاحيات الجذر
السكريبتات التالية تتطلب صلاحيات الجذر:
- `production-restore.sh` - للاستعادة
- إصلاحات الأذونات في `production-helper.sh`

```bash
# تشغيل مع sudo
sudo ./production-restore.sh backup_name
```

### 2. النسخ الاحتياطية
- **دائماً** قم بإنشاء نسخة احتياطية قبل الاستعادة
- تأكد من صحة النسخ الاحتياطية دورياً
- اختبر عمليات الاستعادة في بيئة آمنة

---

## 🔍 استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### 1. خطأ "Permission denied"
```bash
# الحل: إعطاء صلاحيات التنفيذ
chmod +x script-name.sh
```

#### 2. خطأ "Database connection failed"
```bash
# التحقق من إعدادات قاعدة البيانات في السكريبت
# فحص حالة خدمة MySQL
systemctl status mysql

# اختبار الاتصال يدوياً
mysql -u username -p
```

#### 3. خطأ "Application not responding"
```bash
# فحص حالة الخدمات
./production-helper.sh services

# إعادة تشغيل الخدمات
sudo systemctl restart nginx php8.2-fpm
```

---

هذا الدليل يوفر وصفاً شاملاً لاستخدام جميع السكريبتات الإنتاجية. تأكد من قراءة الوثائق الفردية لكل سكريبت للحصول على تفاصيل أكثر.