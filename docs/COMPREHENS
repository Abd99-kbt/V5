# الدليل الشامل لنظام V5 - تشغيل وإدارة
## دليل عملي ومفصل للمطورين والمستخدمين

---

## 📖 مقدمة

هذا الدليل الشامل يغطي جميع جوانب تشغيل وإدارة نظام V5 المبيعات المتقدم. يحتوي على:

- ✅ دليل الإعداد التفصيلي
- 🔧 دليل حل المشاكل الشائعة  
- ⚡ دليل الإرشادات المتقدمة
- 📸 دليل الأمثلة العملية مع لقطات الشاشة

---

## 🚀 البدء السريع

### التشغيل في 5 دقائق

```bash
# 1. تحقق من المتطلبات
php -v
composer --version
node -v

# 2. إعداد المشروع
cp .env.example .env
php artisan key:generate
composer install
npm install

# 3. إعداد قاعدة البيانات
mysql -u root -p -e "CREATE DATABASE v5_development"
php artisan migrate
php artisan db:seed

# 4. تشغيل النظام
./start-local.sh
```

### الوصول للنظام
- **الموقع الرئيسي**: http://localhost:8000
- **لوحة الإدارة**: http://localhost:8000/admin
- **API**: http://localhost:8000/api

### بيانات الدخول الافتراضية
```
المدير: admin@v5-system.com / admin123
المطور: dev@v5-system.com / dev123  
المستخدم: user@v5-system.com / user123
```

---

## 📚 الملفات والمراجع

### 1. الدليل التفصيلي
- **الملف**: `COMPREHENSIVE_SETUP_GUIDE.md`
- **المحتوى**: 
  - فحص المتطلبات الأساسية
  - تثبيت PHP و Composer و Node.js
  - إعداد قاعدة البيانات
  - خطوات التشغيل الدقيقة
  - التحقق من نجاح التشغيل
  - بيانات الدخول التجريبية

### 2. دليل حل المشاكل
- **الملف**: `TROUBLESHOOTING_GUIDE.md`
- **المحتوى**:
  - أخطاء PHP و Composer
  - مشاكل قاعدة البيانات
  - مشاكل Node.js و npm
  - أخطاء في المنافذ
  - مشاكل الصلاحيات
  - مشاكل Authentication
  - مشاكل الأداء

### 3. دليل الإرشادات المتقدمة
- **الملف**: `ADVANCED_GUIDE.md`
- **المحتوى**:
  - التشغيل في وضع التطوير
  - التشغيل في وضع الإنتاج المحلي
  - مراقبة logs
  - استخدام الأدوات المدمجة
  - إعدادات الأمان المتقدمة
  - تحسين الأداء
  - إدارة الإصدارات

### 4. دليل الأمثلة العملية
- **الملف**: `PRACTICAL_EXAMPLES_GUIDE.md`
- **المحتوى**:
  - لقطات شاشة نصية
  - أمثلة واقعية من التشغيل
  - حالات اختبار
  - استكشاف لوحة الإدارة
  - سيناريوهات اليوم العادي

---

## 🔧 الأوامر الأساسية

### إدارة الخادم
```bash
# تشغيل خادم التطوير
php artisan serve

# مع منفذ مخصص
php artisan serve --host=0.0.0.0 --port=8000

# تشغيل Vite
npm run dev

# التشغيل المتقدم
./start-local.sh
```

### إدارة قاعدة البيانات
```bash
# تشغيل migrations
php artisan migrate

# إنشاء migration جديدة
php artisan make:migration create_table_name

# تشغيل seeders
php artisan db:seed

# إنشاء seeder
php artisan make:seeder SeederName
```

### إدارة المستخدمين
```bash
# إنشاء مستخدم
php artisan make:user

# Tinker (وحدة التحكم التفاعلية)
php artisan tinker

# مسح الكاش
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### المراقبة والصيانة
```bash
# فحص حالة النظام
php artisan about

# فحص قاعدة البيانات
php artisan migrate:status

# عرض routes
php artisan route:list

# فحص logs
tail -f storage/logs/laravel.log
```

---

## ⚙️ الإعدادات الأساسية

### ملف .env للإعداد السريع
```env
APP_NAME="V5 Sales System"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=v5_development
DB_USERNAME=v5_user
DB_PASSWORD=your_password

CACHE_DRIVER=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync

LOG_LEVEL=debug
```

### إعدادات MySQL
```sql
-- إنشاء قاعدة البيانات
CREATE DATABASE v5_development;

-- إنشاء مستخدم
CREATE USER 'v5_user'@'localhost' IDENTIFIED BY 'your_password';

-- منح الصلاحيات
GRANT ALL PRIVILEGES ON v5_development.* TO 'v5_user'@'localhost';
FLUSH PRIVILEGES;
```

---

## 🔒 الأمان

### فحص الأمان الأساسي
```bash
# فحص الثغرات
composer audit

# فحص إعدادات الأمان
php artisan security:check

# تحديث التبعيات
composer update
```

### إعدادات الأمان المطلوبة
```env
# في الإنتاج
APP_ENV=production
APP_DEBUG=false
SESSION_SECURE_COOKIE=true
FORCE_HTTPS=true

# كلمات مرور قوية
# تحديث دوري للاعتمادات
# نسخ احتياطية منتظمة
```

---

## 🛠️ استكشاف الأخطاء

### المشاكل الشائعة

#### 1. خطأ في قاعدة البيانات
```bash
# فحص الاتصال
php artisan tinker
>>> DB::connection()->getPdo();

# إعادة تشغيل migrations
php artisan migrate:fresh
```

#### 2. منفذ مشغول
```bash
# العثور على العملية
lsof -ti:8000
# أو
netstat -tulpn | grep 8000

# إنهاء العملية
kill -9 PID_NUMBER

# استخدام منفذ آخر
php artisan serve --port=8080
```

#### 3. خطأ في الصلاحيات
```bash
# إصلاح صلاحيات Linux/macOS
chmod -R 775 storage/
chmod -R 775 bootstrap/cache/

# إصلاح المالك
sudo chown -R www-data:www-data storage/
```

#### 4. مشاكل npm/Node.js
```bash
# مسح cache
npm cache clean --force

# إعادة تثبيت
rm -rf node_modules package-lock.json
npm install
```

### فحص logs الأخطاء
```bash
# Laravel log
tail -f storage/logs/laravel.log

# PHP errors
tail -f /var/log/php_errors.log

# Web server
tail -f /var/log/nginx/error.log
```

---

## 📊 المراقبة والصيانة

### مراقبة الأداء
```bash
# فحص استخدام الذاكرة
php artisan tinker
>>> memory_get_usage(true) / 1024 / 1024

# مراقبة قاعدة البيانات
php artisan db:show

# اختبار الاستجابة
time curl http://localhost:8000
```

### النسخ الاحتياطية
```bash
# نسخ احتياطية قاعدة البيانات
mysqldump -u v5_user -p v5_development > backup_$(date +%Y%m%d).sql

# نسخ ملفات النظام
tar -czf v5_backup_$(date +%Y%m%d).tar.gz storage/ public/
```

### الصيانة الدورية
```bash
# مسح الكاش
php artisan cache:clear
php artisan config:cache
php artisan route:cache
php artisan view:cache

# تحسين قاعدة البيانات
php artisan optimize

# تنظيف ملفات log القديمة
find storage/logs/ -name "*.log" -mtime +30 -delete
```

---

## 🔗 الروابط المهمة

### واجهات النظام
| الخدمة | الرابط | الوصف |
|--------|--------|--------|
| **الصفحة الرئيسية** | http://localhost:8000 | صفحة الدخول الرئيسية |
| **لوحة الإدارة** | http://localhost:8000/admin | لوحة تحكم الإدارة |
| **واجهة API** | http://localhost:8000/api | واجهات برمجة التطبيقات |
| **التوثيق** | http://localhost:8000/docs | وثائق API |
| **مراقبة الصحة** | http://localhost:8000/health | فحص صحة النظام |

### أدوات التطوير
| الأداة | الرابط | الوصف |
|--------|--------|--------|
| **Telescope** | http://localhost:8000/telescope | Laravel Telescope |
| **DebugBar** | http://localhost:8000/?debugbar=1 | Laravel DebugBar |
| **phpMyAdmin** | http://localhost:8080 | إدارة قاعدة البيانات |
| **Vite** | http://localhost:5173 | خادم Vite |

### ملفات الإعدادات
| الملف | المسار | الوصف |
|------|--------|--------|
| **ملف البيئة** | `.env` | متغيرات البيئة |
| **إعدادات Laravel** | `config/app.php` | إعدادات التطبيق |
| **إعدادات قاعدة البيانات** | `config/database.php` | إعدادات DB |
| **إعدادات التخزين** | `config/filesystems.php` | إعدادات التخزين |

---

## 📞 الدعم والمساعدة

### قنوات الدعم
- **البريد الإلكتروني**: support@v5-system.com
- **الدعم التقني**: technical@v5-system.com
- **الطوارئ**: emergency@v5-system.com

### معلومات مفيدة للدعم
عند طلب المساعدة، يرجى تضمين:

```bash
# معلومات النظام
php --version
composer --version
node --v
npm --v

# معلومات Laravel
php artisan --version
php artisan about

# logs الأخطاء
tail -20 storage/logs/laravel.log

# إعدادات البيئة
cat .env | grep -E "APP_|DB_"
```

### مصادر إضافية
- [وثائق Laravel الرسمية](https://laravel.com/docs)
- [وثائق Filament](https://filamentphp.com/docs)
- [مكتبة Laravel العربية](https://laravel-china.org)

---

## 🎯 نصائح للنجاح

### للمبتدئين
1. **اقرأ الأخطاء بعناية** - رسائل الخطأ تحتوي على الحل
2. **استخدم السكريبتات الجاهزة** - ./start-local.sh
3. **احتفظ بنسخ احتياطية** - قبل أي تغييرات مهمة
4. **اختبر في بيئة التطوير** - لا تختبر في الإنتاج

### للمطورين
1. **استخدم IDE جيد** - VS Code أو PhpStorm
2. **فعّل DebugBar** - لفحص الأداء
3. **اكتب اختبارات** - للوظائف الجديدة
4. **استخدم Git** - لإدارة الإصدارات

### للصيانة
1. **راقب السجلات** - بانتظام
2. **اعمل نسخ احتياطية** - منتظمة
3. **حدّث التبعيات** - دورياً
4. **اختبر الأمان** - باستمرار

---

## 📋 قائمة فحص سريعة

### بعد التثبيت
- [ ] تشغيل `composer install` بنجاح
- [ ] تشغيل `npm install` بنجاح  
- [ ] إعداد `.env` بشكل صحيح
- [ ] تشغيل `php artisan migrate` بنجاح
- [ ] تشغيل `php artisan key:generate` بنجاح
- [ ] إنشاء رابط التخزين `storage:link`
- [ ] تعيين الصلاحيات الصحيحة

### قبل الإنتاج
- [ ] تشغيل جميع الاختبارات
- [ ] فحص الأمان
- [ ] إعداد HTTPS
- [ ] نسخ احتياطية
- [ ] مراقبة النظام
- [ ] إعدادات الأداء

### الصيانة اليومية
- [ ] مراقبة السجلات
- [ ] فحص الأداء
- [ ] تحديث التبعيات
- [ ] نسخ احتياطية
- [ ] فحص الأمان

---

## ✅ خاتمة

تم إنشاء هذا الدليل الشامل لضمان نجاح تشغيل وإدارة نظام V5. يحتوي على:

- **دليل الإعداد التفصيلي** - خطوة بخطوة
- **دليل حل المشاكل** - للحلول السريعة
- **دليل الإرشادات المتقدمة** - للمطورين المتقدمين
- **دليل الأمثلة العملية** - لسيناريوهات حقيقية

نتمنى أن يكون هذا الدليل مفيداً في رحلتك مع نظام V5!

---

**📊 إحصائيات الدليل:**
- إجمالي الصفحات: 4 أدلة رئيسية
- عدد الأمثلة: 50+ مثال عملي
- عدد الأوامر: 100+ أمر مفيد
- المشاكل المحلولة: 25+ مشكلة شائعة

**🎯 الجمهور المستهدف:**
- المطورين المبتدئين
- المطورين المتقدمين
- مديري الأنظمة
- فرق الدعم التقني

---

**آخر تحديث**: 2025-11-06  
**رقم الإصدار**: 1.0  
**بواسطة**: فريق تطوير V5