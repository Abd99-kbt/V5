# 🔒 قوائم فحص الأمان الشاملة - نظام V5

## 📋 نظرة عامة

تحتوي هذه الوثيقة على جميع قوائم الفحص الأمنية المطلوبة لضمان أمان النظام في مختلف الأوقات والظروف.

---

## 🚀 قائمة فحص ما قبل النشر (Pre-Deployment)

### 1. فحص البيئة الأساسية
- [ ] **APP_DEBUG=false** في ملف .env
- [ ] **APP_ENV=production** مُحدد بوضوح
- [ ] **APP_KEY** مُولد ومُخفي بشكل آمن
- [ ] **FORCE_HTTPS=true** مُفعل
- [ ] **APP_URL** يستخدم HTTPS ويطابق النطاق الفعلي

### 2. فحص إعدادات قاعدة البيانات
- [ ] **DB_PASSWORD** قوية ومُخفية
- [ ] **DB_SSL_MODE=require** أو **prefer**
- [ ] **DB_CONNECTION_ENCRYPTION=true**
- [ ] **DB_TIMEOUT** مُحدد (3-5 ثواني)
- [ ] قاعدة البيانات ليست على localhost في الإنتاج

### 3. فحص إعدادات Redis
- [ ] **REDIS_PASSWORD** مُعين وآمن
- [ ] **REDIS_PORT** غير افتراضي (غير 6379)
- [ ] **REDIS_CLIENT=phpredis**
- [ ] **CACHE_PREFIX** فريد ومميز
- [ ] قاعدة البيانات منفصلة لكل استخدام

### 4. فحص إعدادات الجلسة
- [ ] **SESSION_DRIVER=redis**
- [ ] **SESSION_ENCRYPT=true**
- [ ] **SESSION_SECURE_COOKIE=true**
- [ ] **SESSION_HTTP_ONLY=true**
- [ ] **SESSION_SAME_SITE=strict** أو **lax**
- [ ] **SESSION_LIFETIME=120** أو أقل

### 5. فحص إعدادات الأمان
- [ ] **BCRYPT_ROUNDS=12** أو أكثر
- [ ] **AUTH_ATTEMPTS_LIMIT=5**
- [ ] **AUTH_LOCKOUT_DURATION=900** (15 دقيقة)
- [ ] **API_RATE_LIMIT=60**
- [ ] **SENSITIVE_RATE_LIMIT=10**

### 6. فحص Middleware الأمان
- [ ] **SecurityHeaders.php** مُسجل في Kernel.php
- [ ] **PreventCommonAttacks.php** مُسجل في Kernel.php
- [ ] **IPBlacklist.php** مُسجل في Kernel.php
- [ ] **CheckIfAdmin.php** للصفحات الحساسة

### 7. فحص الملفات والإذونات
```bash
# فحص أذونات الملفات المهمة
chmod 600 .env
chmod 755 storage/
chmod 755 bootstrap/cache/
chmod 755 storage/app/
chmod 755 storage/framework/
chmod 755 storage/logs/
```

### 8. فحص التبعيات
- [ ] **composer audit** بدون تحذيرات
- [ ] **npm audit** بدون تحذيرات (إن أمكن)
- [ ] لا توجد development dependencies في الإنتاج
- [ ] **composer.lock** موجود ومحدث

### 9. فحص الملفات الحساسة
- [ ] لا توجد ملفات .env.* في الإنتاج
- [ ] لا توجد ملفات debug.php أو test.php
- [ ] لا توجد نسخ احتياطية (.bak, .backup)
- [ ] **.git** مُخفي أو محذوف
- [ ] **.gitignore** يحتوي على المسارات الحساسة

### 10. فحص SSL/TLS
- [ ] شهادة SSL صالحة ومحدثة
- [ ] **HSTS** مُفعل ومُفوض
- [ ] **HTTP to HTTPS** redirection مُفعل
- [ ] **Secure Headers** مُطبقة
- [ ] **Certificate transparency** مُفعل

### 11. فحص إعدادات الخادم
- [ ] **php.ini** مُحسن للأمان:
  ```ini
  expose_php = Off
  display_errors = Off
  log_errors = On
  error_log = /path/to/error.log
  allow_url_fopen = Off
  allow_url_include = Off
  file_uploads = On (مع قيود)
  upload_max_filesize = 10M
  post_max_size = 10M
  max_execution_time = 30
  memory_limit = 128M
  ```
- [ ] **Nginx/Apache** security headers مُفعلة
- [ ] **mod_security** أو مكافئ مُفعل
- [ ] **rate limiting** مُفعل في الخادم

### 12. فحص النسخ الاحتياطي
- [ ] نسخ احتياطية مُجدولة لقاعدة البيانات
- [ ] نسخ احتياطية للملفات المهمة
- [ ] اختبار استعادة النسخ الاحتياطية
- [ ] النسخ الاحتياطية مُشفرة ومحفوظة آمناً

---

## 🏭 قائمة فحص الأمان للإنتاج (Production)

### 1. المراقبة المستمرة
- [ ] **Security logs** تتم مراجعتها يومياً
- [ ] **Error logs** يتم فحصها يومياً
- [ ] **Performance metrics** تتم مراقبتها
- [ ] **Database monitoring** نشط
- [ ] **Redis monitoring** نشط

### 2. فحص محاولات الاختراق
```bash
# فحص محاولات SQL injection
grep -i "union\|select\|drop\|insert" storage/logs/*.log

# فحص محاولات XSS
grep -i "<script\|javascript:" storage/logs/*.log

# فحص محاولات path traversal
grep -i "\.\.\/" storage/logs/*.log

# فحص IP addresses مُريبة
grep -i "blocked\|suspicious" storage/logs/*.log
```

### 3. فحص حالة الجلسات
- [ ] عدد الجلسات النشطة طبيعي
- [ ] لا توجد جلسات معلقة لفترة طويلة
- [ ] **Session hijacking** لم يتم رصده
- [ ] **Remember tokens** آمنة

### 4. فحص معدل الطلبات
- [ ] **API rate limits** لم يتم تجاوزها
- [ ] **Admin endpoints** محمية بشكل إضافي
- [ ] لا توجد أنماط غير طبيعية في الطلبات
- [ ] **DDoS protection** فعال

### 5. فحص قاعدة البيانات
```sql
-- فحص محاولات الدخول المشبوهة
SELECT * FROM mysql.general_log 
WHERE event_time > DATE_SUB(NOW(), INTERVAL 24 HOUR)
AND command_type = 'Connect'
ORDER BY event_time DESC;

-- فحص العمليات الحساسة
SELECT * FROM information_schema.processlist
WHERE command != 'Sleep' 
AND time > 30;
```

### 6. فحص Redis
```bash
# فحص الاتصالات النشطة
redis-cli info clients

# فحص الذاكرة
redis-cli info memory

# فحص العمليات البطيئة
redis-cli slowlog get 10
```

---

## 📅 قائمة الفحص اليومية (Daily Security Checks)

### 1. فحص logs الأمان (9:00 صباحاً)
```bash
# تشغيل فحص الأمان اليومي
php artisan security:daily-check

# أو فحص يدوي
tail -100 storage/logs/laravel.log | grep -E "(SECURITY|ERROR|WARNING)"
```

### 2. فحص حالة الخدمة
- [ ] النظام يعمل بشكل طبيعي
- [ ] لا توجد أخطاء في الـ logs
- [ ] قاعدة البيانات متصلة
- [ ] Redis يعمل
- [ ] جميع APIs تستجيب

### 3. فحص المحاولات المشبوهة
- [ ] محاولات تسجيل دخول فاشلة متعددة
- [ ] IP addresses من مواقع غير مألوفة
- [ ] أنماط طلبات غير عادية
- [ ] محاولات فاشلة للوصول للملفات الحساسة

### 4. فحص النسخ الاحتياطية
- [ ] النسخة الاحتياطية اليومية تمت
- [ ] حجم النسخة طبيعي
- [ ] يمكن قراءة النسخة
- [ ] تُحفظ في موقع آمن

### 5. فحص إحصائيات الأداء
- [ ] زمن الاستجابة طبيعي (< 2 ثانية)
- [ ] استخدام الذاكرة طبيعي
- [ ] استخدام CPU طبيعي
- [ ] مساحة القرص كافية

---

## 📆 قائمة الفحص الأسبوعية (Weekly Security Reviews)

### 1. فحص شامل للـ logs
```bash
# تشغيل تقرير أسبوعي
php artisan security:weekly-report

# فحص محاولات الاختراق
grep -E "(blocked|attack|suspicious)" storage/logs/*.log | wc -l
```

### 2. مراجعة الصلاحيات
- [ ] مراجعة حسابات المستخدمين النشطين
- [ ] حذف الحسابات غير المستخدمة
- [ ] مراجعة صلاحيات الأدوار
- [ ] تحديث كلمات مرور المديرين

### 3. فحص التحديثات
- [ ] **Composer packages** محدثة
- [ ] **npm packages** محدثة (إن أمكن)
- [ ] **PHP version** محدثة
- [ ] **Server OS** محدثة

### 4. فحص إعدادات الأمان
- [ ] مراجعة **rate limits**
- [ ] تحديث **IP blacklist**
- [ ] مراجعة **security headers**
- [ ] فحص **SSL certificates**

### 5. اختبار اختبارات الأمان
```bash
# تشغيل جميع اختبارات الأمان
php artisan test tests/Security/

# اختبار يدوي للثغرات
php artisan security:manual-scan
```

---

## 📊 قائمة الفحص الشهرية (Monthly Security Audits)

### 1. مراجعة شاملة للأمان
- [ ] تشغيل **penetration testing** أساسي
- [ ] مراجعة **threat model**
- [ ] تحديث **security policies**
- [ ] مراجعة **incident response plan**

### 2. تحديث كلمة المرور الإجبارية
- [ ] إجبار المديرين على تغيير كلمات المرور
- [ ] مراجعة **password policies**
- [ ] فحص **password history**
- [ ] تحديث **MFA secrets**

### 3. مراجعة الأداء الأمني
```bash
# تقرير أداء شامل
php artisan security:performance-report

# تحليل استخدام الموارد
php artisan system:analyze-usage
```

### 4. فحص الـ Compliance
- [ ] **GDPR compliance** مُحسن
- [ ] **Data retention policies** محدثة
- [ ] **Privacy policies** محدثة
- [ ] **Security certifications** محدثة

### 5. تدريب الفريق
- [ ] ورشة عمل أمنية للفريق
- [ ] تحديث **security procedures**
- [ ] مراجعة **incident response procedures**
- [ ] تحديث **emergency contacts**

---

## 🚨 قائمة فحص الطوارئ (Emergency Security Response)

### عند اكتشاف اختراق أمني
- [ ] **إيقاف النظام فوراً** (إذا كان خطيراً)
- [ ] **تغيير جميع كلمات المرور** الحساسة
- [ ] **فحص الـ logs** لمعرفة مد范围 الاختراق
- [ ] **تحديث security patches** عاجلة
- [ ] **إشعار الفريق الأمني** والمديرين

### عند اكتشاف ثغرة أمنية
- [ ] **فحص الثغرة** وتحديد شدتها
- [ ] **تطبيق patch عاجل** (إن أمكن)
- [ ] **تعطيل الميزة المصابة** (إذا لزم الأمر)
- [ ] **إشعار المستخدمين** (إذا كان يؤثر عليهم)
- [ ] **توثيق الثغرة** والحل

---

## 📋 قوائم الفحص التلقائية

### نص Bash للفحص اليومي
```bash
#!/bin/bash
# daily-security-check.sh

echo "🔒 Daily Security Check - $(date)"

# فحص logs للأخطاء
echo "📋 Checking logs..."
tail -100 storage/logs/laravel.log | grep -E "(ERROR|SECURITY|WARNING)" | wc -l

# فحص IP addresses المحجوبة
echo "🌐 Checking blocked IPs..."
redis-cli smembers "blacklist:*" | wc -l

# فحص session count
echo "👤 Checking active sessions..."
redis-cli scard "session:*" 

# فحص memory usage
echo "💾 Checking memory usage..."
free -h

echo "✅ Daily check completed"
```

### نص PHP للفحص التقني
```php
<?php
// daily-security-check.php

echo "🔒 Daily Technical Security Check\n";

try {
    // فحص قاعدة البيانات
    $db = new PDO("mysql:host=localhost;dbname=V5", "root", "");
    $result = $db->query("SELECT COUNT(*) FROM users WHERE last_login_at < DATE_SUB(NOW(), INTERVAL 30 DAY)");
    $inactiveUsers = $result->fetchColumn();
    echo "👥 Inactive users (30+ days): $inactiveUsers\n";
    
    // فحص Redis
    $redis = new Redis();
    $redis->connect('127.0.0.1', 6379);
    $blockedIPs = $redis->scard('blacklist:*');
    echo "🚫 Blocked IPs: $blockedIPs\n";
    
    echo "✅ Technical check completed\n";
    
} catch (Exception $e) {
    echo "❌ Error: " . $e->getMessage() . "\n";
}
```

---

## 📞 أرقام الطوارئ والأرقام المهمة

### فريق الأمان
- **مدير الأمان**: +963-XXX-XXXX
- **فريق التطوير**: +963-XXX-XXXX
- **دعم الخوادم**: +963-XXX-XXXX

### الجهات الخارجية
- **مزود الاستضافة**: +963-XXX-XXXX
- **مزود SSL**: +963-XXX-XXXX
- **مزود قاعدة البيانات**: +963-XXX-XXXX

### الاستجابة للطوارئ
- **البريد الإلكتروني للأمان**: security@v5-system.com
- **تليجرام**: @V5SecurityTeam
- **Slack**: #security-alerts

---

**تم إنشاء هذه القوائم بواسطة**: فريق الأمان V5  
**تاريخ الإنشاء**: 2025-11-06  
**آخر تحديث**: 2025-11-06  
**الإصدار**: 1.0

> **ملاحظة**: يجب مراجعة وتحديث هذه القوائم بانتظام (شهرياً على الأقل) لتضمن مواكبة التهديدات الأمنية الحديثة.