# 🚨 دليل استكشاف الأخطاء - نظام V5
## نظام التشخيص والإصلاح التلقائي

---

## ⚡ الحل السريع - استخدم هذه الأوامر أولاً!

### التشخيص التلقائي
```bash
# تشخيص شامل لجميع المشاكل
diagnose.bat
```

### الإصلاح التلقائي
```bash
# إصلاح تلقائي للمشاكل الشائعة
fix-issues.bat
```

### التشغيل البسيط
```bash
# تشغيل مبسط بدون فحوصات معقدة
run-simple.bat
```

---

## 📋 المحتويات

1. [التشخيص التلقائي](#-التشخيص-التلقائي)
2. [الإصلاح التلقائي](#-الإصلاح-التلقائي)
3. [أخطاء شائعة وحلولها](#-أخطاء-شائعة-وحلولها)
4. [أدوات التشخيص المتقدمة](#-أدوات-التشخيص-المتقدمة)
5. [طلب المساعدة](#-طلب-المساعدة)

---

## 🔍 التشخيص التلقائي

### `diagnose.bat` - الفحص الشامل

هذا السكريبت يفحص:
- ✅ البرمجيات المطلوبة (PHP, Composer, Node.js, MySQL)
- ✅ إعدادات قاعدة البيانات والاتصال
- ✅ المنافذ المطلوبة (8000, 5173, 3306)
- ✅ ملفات المشروع والصلاحيات
- ✅ حالة Laravel والكاش

**الاستخدام:**
```bash
diagnose.bat
```

**ما ينتجه:**
- تقرير مفصل محفوظ في `diagnostic_report_YYYYMMDD_HHMMSS.txt`
- قائمة بالمشاكل المكتشفة مع الحلول المقترحة
- رموز خروج تشير لحالة النظام

---

## 🔧 الإصلاح التلقائي

### `fix-issues.bat` - الإصلاح التلقائي

هذا السكريبت يصلح تلقائياً:
- ✅ إنشاء وتحديث ملف `.env`
- ✅ إصلاح صلاحيات المجلدات
- ✅ إعادة تثبيت التبعيات
- ✅ توليد مفاتيح Laravel وإنشاء روابط
- ✅ مسح الكاش وإعادة التهيئة
- ✅ إيقاف العمليات على المنافذ المطلوبة
- ✅ تنظيف الملفات المؤقتة

**الاستخدام:**
```bash
fix-issues.bat
```

**المميزات:**
- إنشاء نسخ احتياطية تلقائية
- تقرير مفصل بالإصلاحات المطبقة
- معالجة الأخطاء وعدم التوقف عند الفشل

---

## 🚀 التشغيل البسيط

### `run-simple.bat` - التشغيل السريع

للمستخدمين الذين يريدون تشغيل النظام بسرعة:
- ✅ فحص سريع للمتطلبات الأساسية
- ✅ رسائل خطأ واضحة باللغة العربية
- ✅ خيارات بديلة للمشاكل الشائعة
- ✅ تشغيل مبسط بدون فحوصات معقدة

**الاستخدام:**
```bash
run-simple.bat
```

---

## 🐛 أخطاء شائعة وحلولها

### 1. "النظام لا يعمل أبداً"

**الأعراض:**
- رسائل خطأ متعددة
- فشل في بدء الخوادم
- مشاكل في قاعدة البيانات

**الحل السريع:**
```bash
# خطوة 1: تشخيص المشاكل
diagnose.bat

# خطوة 2: إصلاح تلقائي
fix-issues.bat

# خطوة 3: تشغيل بسيط
run-simple.bat
```

### 2. "Port already in use"

**الأعراض:**
- "Port 8000 already in use"
- "Port 5173 already in use"

**الحلول:**
```bash
# الحل التلقائي
fix-issues.bat

# أو يدوياً - إيقاف العمليات
# Windows
netstat -ano | findstr :8000
taskkill /PID <PID_NUMBER> /F

# Linux/Mac
lsof -ti:8000 | xargs kill -9
```

### 3. "Database connection failed"

**الأعراض:**
- "SQLSTATE[HY000] [2002] Connection refused"
- "Access denied for user"

**التحقق:**
```bash
# فحص خدمة MySQL
# Windows: services.msc
# Linux: sudo systemctl status mysql

# اختبار الاتصال
php artisan tinker
>>> DB::connection()->getPdo();
```

**الحل:**
```bash
# إنشاء قاعدة البيانات
mysql -u root -p
CREATE DATABASE v5_development CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
GRANT ALL PRIVILEGES ON v5_development.* TO 'v5_user'@'localhost' IDENTIFIED BY 'password';
FLUSH PRIVILEGES;
EXIT;

# تحديث .env
DB_DATABASE=v5_development
DB_USERNAME=v5_user
DB_PASSWORD=password
```

### 4. "Class not found" / "Artisan not found"

**الحل:**
```bash
# إعادة تثبيت التبعيات
composer install
composer dump-autoload

# أو الإصلاح التلقائي
fix-issues.bat
```

### 5. "Permission denied"

**الحل:**
```bash
# Windows
fix-issues.bat

# أو يدوياً
icacls storage /grant Users:F /T
icacls bootstrap\cache /grant Users:F /T
```

### 6. "Out of memory" / "Allowed memory size exhausted"

**الحل:**
```bash
# زيادة ذاكرة PHP
# في php.ini:
memory_limit = 512M

# أو للأمر الحالي
php -d memory_limit=512M artisan command

# تنظيف Composer cache
composer clear-cache
```

---

## 🔧 أدوات التشخيص المتقدمة

### فحص مفصل للنظام
```bash
# معلومات PHP
php -v
php -m | grep -E "mysql|mbstring|curl|zip"

# معلومات Composer
composer --version
composer show

# معلومات Node.js
node -v
npm -v
npm list

# معلومات قاعدة البيانات
php artisan migrate:status
php artisan db:monitor
```

### مراقبة السجلات
```bash
# عرض آخر الأخطاء
tail -50 storage/logs/laravel.log

# مراقبة السجل في الوقت الفعلي
tail -f storage/logs/laravel.log

# فحص سجلات النظام
# Windows: Event Viewer
# Linux: journalctl -u mysql
```

### اختبارات الاتصال
```bash
# اختبار قاعدة البيانات
php artisan tinker
>>> DB::connection()->getPdo()
>>> User::count()

# اختبار Redis (إذا كان مستخدماً)
php artisan tinker
>>> Redis::ping()

# اختبار البريد (إذا كان مستخدماً)
php artisan tinker
>>> Mail::raw('Test', function($m) { $m->to('test@example.com'); });
```

---

## 📊 فهم رموز الخروج

| رمز | معنى | الإجراء |
|-----|------|---------|
| 0 | نجاح | النظام يعمل بشكل صحيح |
| 1 | فشل عام | تشغيل `diagnose.bat` |
| 2 | متطلبات مفقودة | تثبيت البرمجيات المطلوبة |
| 3 | مشاكل قاعدة البيانات | فحص إعدادات MySQL |
| 4 | مشاكل المنافذ | تشغيل `fix-issues.bat` |
| 5 | مشاكل الصلاحيات | تشغيل `fix-issues.bat` |

---

## 🚨 مشاكل طارئة

### النظام لا يبدأ أبداً
```bash
# إعادة تعيين كاملة
fix-issues.bat
run-simple.bat
```

### قاعدة البيانات تالفة
```bash
# إعادة إنشاء قاعدة البيانات
php artisan migrate:fresh --seed
```

### جميع الخدمات متوقفة
```bash
# Windows
services.msc
# تشغيل: MySQL, Apache (إذا كان مستخدماً)

# Linux
sudo systemctl start mysql
sudo systemctl start apache2
```

---

## 📞 طلب المساعدة

### قبل طلب المساعدة، اجمع هذه المعلومات:

```bash
# إنشاء تقرير شامل
echo "=== System Info ===" > support-report.txt
echo "Date: $(date)" >> support-report.txt
php -v >> support-report.txt 2>&1
composer --version >> support-report.txt 2>&1
node -v >> support-report.txt 2>&1
mysql --version >> support-report.txt 2>&1

echo "=== Laravel Info ===" >> support-report.txt
php artisan --version >> support-report.txt 2>&1
php artisan about >> support-report.txt 2>&1

echo "=== Recent Errors ===" >> support-report.txt
tail -20 storage/logs/laravel.log >> support-report.txt 2>&1

echo "=== Environment ===" >> support-report.txt
cat .env >> support-report.txt 2>&1

echo "Report saved to support-report.txt"
```

### معلومات مفيدة للدعم:
- ✅ نتائج `diagnose.bat`
- ✅ ملف `support-report.txt`
- ✅ وصف دقيق للمشكلة
- ✅ خطوات إعادة إنتاج الخطأ
- ✅ لقطات شاشة للأخطاء
- ✅ إصدارات البرمجيات

### عناوين الدعم:
- 📧 **الدعم الفني**: support@v5-system.com
- 🚨 **الطوارئ**: emergency@v5-system.com
- 🔒 **الأمان**: security@v5-system.com

---

## ⚡ نصائح سريعة

1. **ابدأ دائماً بالتشخيص**: `diagnose.bat`
2. **استخدم الإصلاح التلقائي**: `fix-issues.bat`
3. **اقرأ رسائل الخطأ بعناية**: غالباً ما تحتوي على الحل
4. **احتفظ بنسخ احتياطية**: قبل إجراء تغييرات كبيرة
5. **جرب الحلول تدريجياً**: حل واحد في كل مرة

---

## 📋 قائمة مرجعية لاستكشاف الأخطاء

- [ ] تشغيل `diagnose.bat`
- [ ] مراجعة التقرير المحفوظ
- [ ] تشغيل `fix-issues.bat`
- [ ] محاولة `run-simple.bat`
- [ ] فحص سجلات Laravel
- [ ] اختبار اتصال قاعدة البيانات
- [ ] فحص المنافذ المستخدمة
- [ ] التحقق من الصلاحيات

---

**🎯 تذكر**: معظم المشاكل تحل تلقائياً باستخدام `diagnose.bat` و `fix-issues.bat`!

**آخر تحديث**: 2025-11-06
**الإصدار**: 2.0 - نظام التشخيص التلقائي