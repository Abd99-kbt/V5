# 🚀 دليل النشر الشامل لمشروع V5 على Ubuntu 25

## 📋 نظرة عامة

هذا السكريبت يقوم بنشر مشروع V5 (نظام إدارة المبيعات والمخازن) بشكل تلقائي وشامل على Ubuntu 25 مع جميع التكوينات المطلوبة للإنتاج.

## ⚡ المميزات

- ✅ **تثبيت تلقائي شامل**: PHP 8.2, MySQL, Nginx, Node.js
- 🔒 **أمان متقدم**: SSL مجاني, Firewall, مراقبة هجمات  
- 📊 **مراقبة النظام**: Fail2ban, UFW, logs
- 💾 **نسخ احتياطية تلقائية**: يومياً مع الاحتفاظ لمدة 7 أيام
- 🔧 **تحكم سهل**: سكريبت إدارة للتشغيل/الإيقاف
- 📈 **تحسينات الأداء**: Redis cache, PHP OPcache, Nginx gzip

## 🛠️ المتطلبات

- Ubuntu 25 (أو أحدث)
- صلاحيات root (sudo)
- اتصال إنترنت مستقر
- نطاق (domain) مُسجل

## 📦 التثبيت

### 1. رفع المشروع إلى الخادم
```bash
# انسخ جميع ملفات V5 إلى الخادم
scp -r V5/ root@your-server-ip:/tmp/

# أو استخدم Git
git clone your-v5-repo /tmp/V5
```

### 2. نقل الملفات للموقع
```bash
# على الخادم
cd /tmp/V5
sudo cp -r . /var/www/v5/
sudo rm -rf /tmp/V5
```

### 3. تشغيل السكريبت
```bash
# أعط صلاحيات التنفيذ
sudo chmod +x deploy-ubuntu25.sh

# شغل السكريبت
sudo ./deploy-ubuntu25.sh
```

## ⚙️ التكوينات

### المتغيرات الأساسية (تعديل في السكريبت)
```bash
PROJECT_NAME="V5"                    # اسم المشروع
PROJECT_PATH="/var/www/v5"           # مسار المشروع
DB_NAME="v5_production"              # اسم قاعدة البيانات
DB_USER="v5_user"                    # مستخدم قاعدة البيانات
APP_URL="https://your-domain.com"    # رابط الموقع
WEB_USER="www-data"                  # مستخدم الويب
PHP_VERSION="8.2"                    # إصدار PHP
```

## 🔧 الخدمات المُثبتة

### 1. PHP 8.2
- **المكونات**: FPM, MySQL, Redis, GD, Imagick, OpCache
- **الأداء**: مُحسن للإنتاج مع OPcache مفعّل

### 2. MySQL 8.0
- **الأمان**: مُؤمن مع كلمة مرور قوية
- **قاعدة البيانات**: أنشأت تلقائياً مع مستخدم محدود الصلاحيات

### 3. Nginx
- **HTTPS**: SSL مجاني مع Let's Encrypt
- **الأمان**: Headers أمنية، Rate limiting
- **الأداء**: Gzip compression، Static file caching

### 4. Redis
- **Cache**: للتطبيق والجلسات والـ queues
- **الأداء**: تحسينات في سرعة التحميل

### 5. Node.js 22
- **الحزم**: npm, yarn للتطوير والبناء

## 🔐 الأمان

### Firewall (UFW)
```bash
# يسمح بالوصول فقط لـ:
- SSH (port 22)
- HTTP/HTTPS (ports 80/443)
- MySQL (localhost فقط)
```

### مراقبة الهجمات (Fail2ban)
- حماية من Brute Force attacks
- حظر IP تلقائي بعد محاولات فاشلة متعددة

### SSL Certificate
- شهادة SSL مجانية من Let's Encrypt
- تجديد تلقائي شهرياً

## 💾 النسخ الاحتياطية

### التكوين التلقائي
- **التكرار**: يومياً في الساعة 2:00 صباحاً
- **الاحتفاظ**: 7 أيام
- **المحتوى**: 
  - قاعدة البيانات (SQL dump)
  - جميع ملفات المشروع (tar.gz)

### الملفات المؤقتة
```bash
# معلومات قاعدة البيانات
/root/database_info.txt

# سكريبت النسخ الاحتياطية
/usr/local/bin/backup-v5.sh

# مجلد النسخ الاحتياطية
/var/backups/v5/
```

## 🛠️ إدارة النظام

### سكريبت التحكم
```bash
# بدء الخدمات
sudo v5-control start

# إيقاف الخدمات  
sudo v5-control stop

# إعادة تشغيل
sudo v5-control restart

# عرض الحالة
sudo v5-control status

# عرض السجلات
sudo v5-control logs
```

### إدارة قاعدة البيانات
```bash
# الوصول لقاعدة البيانات
mysql -u v5_user -p v5_production

# استعادة نسخة احتياطية
mysql -u v5_user -p v5_production < /var/backups/v5/db_backup_YYYYMMDD_HHMMSS.sql
```

### إدارة السجلات
```bash
# سجلات Nginx
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log

# سجلات PHP
sudo tail -f /var/log/php8.2-fpm.log

# سجلات Laravel
sudo tail -f /var/www/v5/storage/logs/laravel.log

# سجلات النظام
sudo journalctl -u nginx -f
sudo journalctl -u mysql -f
```

## 🌐 الروابط المهمة

### بعد النشر
- **الموقع الرئيسي**: `https://your-domain.com`
- **لوحة الإدارة**: `https://your-domain.com/admin`

### لوحة التحكم الداخلية
- **SSL Management**: `/etc/letsencrypt/`
- **Nginx Config**: `/etc/nginx/sites-available/v5`
- **PHP Config**: `/etc/php/8.2/`

## 🔧 تخصيص إضافي

### تغيير إعدادات PHP
```bash
# تحرير ملف التكوين
sudo nano /etc/php/8.2/fpm/php.ini

# إعادة تشغيل PHP-FPM
sudo systemctl restart php8.2-fpm
```

### إضافة نطاق إضافي
```bash
# تحرير إعدادات Nginx
sudo nano /etc/nginx/sites-available/v5

# إضافة server block جديد
# ثم إعادة تحميل Nginx
sudo nginx -t && sudo systemctl reload nginx
```

### تخصيص SSL
```bash
# إضافة نطاق إضافي لـ SSL
sudo certbot --nginx -d additional-domain.com

# تجديد يدوي
sudo certbot renew
```

## 🐛 حل المشاكل

### مشاكل شائعة

#### 1. خطأ في اتصال قاعدة البيانات
```bash
# تحقق من حالة MySQL
sudo systemctl status mysql

# تحقق من إعدادات .env
sudo nano /var/www/v5/.env

# اختبر الاتصال
mysql -u v5_user -p v5_production
```

#### 2. خطأ في صلاحيات الملفات
```bash
# إصلاح صلاحيات المشروع
sudo chown -R www-data:www-data /var/www/v5
sudo chmod -R 755 /var/www/v5
sudo chmod -R 775 /var/www/v5/storage
sudo chmod -R 775 /var/www/v5/bootstrap/cache
```

#### 3. خطأ في SSL
```bash
# تحقق من شهادة SSL
sudo certbot certificates

# تجديد يدوي
sudo certbot renew --force-renewal
```

#### 4. مشاكل الأداء
```bash
# تحقق من استخدام الذاكرة
free -h

# تحقق من استخدام القرص
df -h

# تحقق من العمليات النشطة
htop
```

### فحص النظام
```bash
# فحص شامل للنظام
sudo bash -c '
echo "=== System Status ==="
systemctl status nginx
systemctl status mysql  
systemctl status php8.2-fpm
systemctl status redis-server

echo "=== Disk Usage ==="
df -h

echo "=== Memory Usage ==="
free -h

echo "=== Nginx Logs ==="
tail -5 /var/log/nginx/error.log
'
```

## 📊 مراقبة الأداء

### Metrices مهمة
- **Response Time**: < 200ms
- **Uptime**: 99.9%+
- **Memory Usage**: < 80%
- **Disk Usage**: < 85%

### أدوات المراقبة
```bash
# مراقبة الموارد
htop
iotop
nethogs

# مراقبة الشبكة
netstat -tulpn | grep :80
netstat -tulpn | grep :443

# مراقبة MySQL
mysql -e "SHOW PROCESSLIST;"
mysql -e "SHOW STATUS LIKE 'Connections';"
```

## 🔄 التحديثات

### تحديث النظام
```bash
# تحديث الحظام
sudo apt update && sudo apt upgrade -y

# تحديث SSL certificates
sudo certbot renew
```

### تحديث المشروع
```bash
# نسخ احتياطية أولاً
sudo /usr/local/bin/backup-v5.sh

# تحديث الكود
cd /var/www/v5
sudo git pull origin main

# تحديث التبعيات
sudo composer install --optimize-autoloader --no-dev
sudo npm ci --production
sudo npm run build

# تشغيل migrations الجديدة
sudo php artisan migrate --force

# إعادة تشغيل الخدمات
sudo systemctl restart php8.2-fpm
sudo systemctl reload nginx
```

## 🎯 نصائح للإنتاج

### الأمان
1. **غيّر كلمات المرور الافتراضية فوراً**
2. **فعّل Two-Factor Authentication**
3. **راقب السجلات بانتظام**
4. **حدّث النظام بانتظام**

### الأداء
1. **استخدم Redis للـ caching**
2. **فعّل OPcache**
3. **استخدم CDN للمحتوى الثابت**
4. **راقب استخدام الذاكرة**

### النسخ الاحتياطية
1. **اختبر النسخ الاحتياطية بانتظام**
2. **احتفظ بنسخ في مواقع متعددة**
3. **وثق عملية الاستعادة**

## 📞 الدعم

للحصول على المساعدة:
1. راجع السجلات أولاً
2. تحقق من حالة الخدمات
3. راجع هذا الدليل
4. تواصل مع فريق الدعم

---

**🎉 مبروك! مشروع V5 جاهز للاستخدام على Ubuntu 25**

**تاريخ الإنشاء**: 2025-11-13  
**الإصدار**: 1.0  
**المنصة**: Ubuntu 25, PHP 8.2, MySQL 8.0, Nginx